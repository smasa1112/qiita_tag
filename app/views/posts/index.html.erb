<h1>投稿一覧</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>投稿内容</th>
      <th>付属タグ一覧</th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.id %></td>
        <td><%= post.content %></td>
        <td>
          <% post.tags.each do |tag| %>
            <%= tag.name %><br>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<h1>新規投稿</h1>
<%= form_with model:@new_post, url: posts_path, local: true do |f| %>
  <%= f.label :content, "投稿内容" %>
  <%= f.text_field :content %>
  
  <%= f.label :tag_strs, "付属したいタグ" %>
  <%= f.text_field :tag_strs %>
  <p><b>タグはカンマ(,)区切りで入力してください！</b></p>
<% end 