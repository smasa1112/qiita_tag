<h1> タグ一覧 </h1>
<table>
  <thead>
    <tr>
      <th>タグ名</th>
      <th>結びついた投稿数</th>
    </tr>
  </thead>
  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td><%= tag.name %></td>
        <td><%= tag.posts %></td>
      </tr>
    <% end %>
  </tbody>
</table>